# Build and push the custom OpenClaw agent image to Quay.io.
#
# Triggers:
#   - Push to image/ on main (tool list changed)
#   - Manual dispatch (pin to a specific upstream version)
#   - Weekly schedule (pick up upstream updates)
#
# Required repository secrets:
#   QUAY_USERNAME  — Quay.io robot account username
#   QUAY_PASSWORD  — Quay.io robot account token

name: Build Agent Image

on:
  push:
    branches: [main]
    paths:
      - "image/**"
  workflow_dispatch:
    inputs:
      upstream_tag:
        description: "Upstream OpenClaw tag (e.g. 2026.7.1). Leave empty for latest."
        required: false
        default: ""
  schedule:
    # Weekly Monday 06:07 UTC — offset from :00 to avoid GitHub Actions congestion
    - cron: "7 6 * * 1"

env:
  REGISTRY: quay.io
  # TODO: change to your Quay.io organization/repository
  IMAGE_NAME: redhat-et/openclaw-agent

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Resolve upstream version
        id: upstream
        run: |
          TAG="${{ inputs.upstream_tag }}"
          if [ -z "$TAG" ]; then
            # Pull latest and read the version from OCI labels
            docker pull ghcr.io/openclaw/openclaw:latest >/dev/null 2>&1
            TAG="$(docker inspect ghcr.io/openclaw/openclaw:latest \
              --format '{{ index .Config.Labels "org.opencontainers.image.version" }}')"
            if [ -z "$TAG" ]; then
              echo "::warning::Could not detect upstream version, falling back to date tag"
              TAG=""
            fi
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "build_tag=${TAG:-latest}" >> "$GITHUB_OUTPUT"
          echo "Upstream version: ${TAG:-unknown (using latest)}"

      - name: Generate image metadata
        id: meta
        run: |
          DATE="$(date -u +%Y%m%d)"
          VERSION="${{ steps.upstream.outputs.tag }}"
          FULL="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"

          TAGS="${FULL}:latest"
          if [ -n "$VERSION" ]; then
            TAGS="${TAGS},${FULL}:${VERSION},${FULL}:${VERSION}-${DATE}"
          else
            TAGS="${TAGS},${FULL}:${DATE}"
          fi
          echo "tags=$TAGS" >> "$GITHUB_OUTPUT"

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.QUAY_USERNAME }}
          password: ${{ secrets.QUAY_PASSWORD }}

      - uses: docker/build-push-action@v6
        with:
          context: image/
          file: image/Containerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          build-args: |
            UPSTREAM=ghcr.io/openclaw/openclaw:${{ steps.upstream.outputs.build_tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
